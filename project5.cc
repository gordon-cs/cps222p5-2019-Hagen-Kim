#include "graph.cc"
#include <iostream>
#include <queue>
#include <list>
#include <string>
#include <map>
#include <vector>

// Main function
int main()
{
  ifstream file;
  file.open(file);
  readFile(ifstream fileInput);
  print();
}
